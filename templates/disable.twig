{% extends "base.twig" %}

{% block content %}
    <h1>{{ 'Turned off ability to use automatic login for this browser'|trans }}</h1>
    <br />
    {{ '{negotiate:negotiate:disable_info_pre}'|trans({'%URL%': url}, "app")|raw }}
    <br />
    {{ '{negotiate:negotiate:info_post}'|trans|raw }}
{% endblock %}
